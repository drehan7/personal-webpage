import classes from './Projects.module.css'

const ProjComponent = (props) => {
    return (
        <div id={props.key} className={classes.ProjComponent}>


        </div>
    )
}

export default ProjComponent
